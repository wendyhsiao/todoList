<a href="/todos/new">Create</a>

<ul>
  {{#each todos}}
  <li>
    {{{this.name}}}
    <a href="/todos/{{{this.id}}}">detail</a>  //this._id也可以??
    <a href="#">edit</a>
    <button type="submit">delete</button>
  </li>
  {{/each}}
</ul>